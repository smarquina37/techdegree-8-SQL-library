extends layout

block content
  h1= `Server Error`
  h2= `Error status code: ${err.status} - Server Error`
  p.error-p= `${err.message}`

  a.home(href=`/`) Home

  div.not-found-stack
    pre #{err.stack}
